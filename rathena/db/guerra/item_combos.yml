###########################################################################
# Guerra do Emperium - Conjuntos proprios
###########################################################################
#
# Este arquivo e NOSSO. Ele e encadeado a partir do `Footer: Imports:` de
# db/item_combos.yml, que so ganhou uma linha apontando para ca.
#
# Por que aqui e nao em db/import/item_combos.yml: o .gitignore do proprio
# rAthena ignora /db/import (rathena/.gitignore, linha 67). Um conjunto
# definido la ficaria fora do git e sumiria num clone limpo. Mesma razao,
# e mesma solucao, do db/guerra/item_db.yml.
#
###########################################################################
# NASCEU EM 2026-08-05, COM A DIADEMA DO PARAISO COM COVA
###########################################################################
#
# A loja do Ocleiro (npc/guerra/mercado_contemporaneo.txt) trocou o 19024
# pelo 19455, que e a mesma Diadema do Paraiso com um encaixe. O 19455 nao
# existe no nosso rAthena vendorizado e entrou em db/guerra/item_db.yml.
#
# So que TROCAR O ID QUEBRA CONJUNTO. Conjunto no rAthena e casado por
# AegisName, nao por familia - a versao com cova e outro item, e ponto.
#
# O rAthena sabe disso e tem receita para isso: onde ele conhece as duas
# versoes, ele lista a com cova como ALTERNATIVA dentro do mesmo
# `- Combos:`, duas listas `- Combo:` compartilhando um Script so. E o
# caso dos outros tres itens que a loja trocou no mesmo dia - 19444,
# 19446 e 410125 estao la, ao lado das versoes sem cova, e por isso nao
# precisaram de nada.
#
# Com a Diadema nao dava, porque o 19455 nao existe no vendor: nao ha
# alternativa a listar. O db/re/item_combos.yml cita `Protect_Feathers`
# (19024) em tres conjuntos:
#
#   1) Boina Alada (5170) + Diadema        -> loja do Chapeleiro
#   2) Diadema + Traje Protetor (19381)    -> loja do Retoqueiro
#   3) Diadema + Traje + Escudo Protetor (28985)
#
# Duas das tres pecas estao a venda no mesmo mercado, a poucos passos do
# Ocleiro. Sem este arquivo, quem comprasse a versao com cova - que custa
# mais e existe justamente para ser melhor - PERDERIA bonus em relacao a
# sem cova, e sem nenhum aviso: conjunto que nao fecha nao da erro, so nao
# acontece. O 28985 nao esta no mercado, mas foi espelhado igual: o motivo
# de espelhar e o ID ter mudado, e isso vale para o item onde quer que ele
# venha a cair.
#
# Cada Script abaixo e copia do conjunto correspondente do
# db/re/item_combos.yml, sem uma virgula de diferenca - a unica mudanca e
# `Protect_Feathers` virar `Protect_Feathers_`. Se um dia o rAthena mudar
# os numeros dele, ESTES AQUI NAO MUDAM SOZINHOS.
#
# Sao entradas separadas, e nao a alternativa dentro do `- Combos:` deles,
# porque de um arquivo de import nao se acrescenta linha a uma entrada
# alheia. O efeito para o jogador e o mesmo: so um equipamento de meio
# cabe por vez, entao ou fecha o conjunto da versao sem cova ou o da com
# cova, nunca os dois.
#
# **Ao atualizar o rathena/, conferir os tres contra o db/re/**, junto com
# as secoes de placeholder e de override do db/guerra/item_db.yml. E se o
# vendor um dia trouxer o 19455 de verdade, ele provavelmente trara estes
# conjuntos junto - e ai os daqui viram duplicata e devem sair.
#
###########################################################################

Header:
  Type: COMBO_DB
  Version: 1

Body:
  # ----- Boina Alada + Diadema do Paraiso [1] --------------------------
  # Espelha db/re/item_combos.yml, o conjunto 5170 + 19024.
  # O refino lido e o do EQUIPAMENTO DE TOPO (a Boina), nao o da Diadema.
  - Combos:
      - Combo:
          - Feather_Beret    # 5170
          - Protect_Feathers_    # 19455
    Script: |
      .@r = getequiprefinerycnt(EQI_HEAD_TOP);
      if (.@r >= 7) {
         bonus bAspdRate,5;
         if (.@r >= 9) {
            bonus bMaxHPrate,5;
         }
      }

  # ----- Diadema do Paraiso [1] + Traje Protetor -----------------------
  # Espelha db/re/item_combos.yml, o conjunto 19024 + 19381.
  - Combos:
      - Combo:
          - Protect_Feathers_    # 19455
          - Protect_cloth    # 19381
    Script: |
      bonus bMdef,5;
      bonus bDef,50;
      bonus bAspdRate,5;
      bonus bVit,5;
      bonus2 bSubRace,RC_Player_Human,3;
      if (readparam(bVit) >= 108) {
         bonus bMdef,5;
         bonus bDef,50;
         bonus bAspdRate,5;
         bonus bVit,5;
      }
      if (readparam(bVit) >= 120) {
         bonus bMdef,10;
         bonus bDef,100;
         bonus bVit,10;
      }

  # ----- Diadema [1] + Traje Protetor + Escudo Protetor ----------------
  # Espelha db/re/item_combos.yml, o conjunto 19024 + 19381 + 28985.
  - Combos:
      - Combo:
          - Protect_Feathers_    # 19455
          - Protect_cloth    # 19381
          - Protect_Shield_TH    # 28985
    Script: |
      bonus2 bSubSkill,"WL_CRIMSONROCK",30;
      bonus2 bSubSkill,"NC_ARMSCANNON",30;
      bonus2 bSubSkill,"RA_ARROWSTORM",30;
      bonus2 bSubSkill,"SR_HELLGATE",30;
      bonus2 bSubSkill,"SU_LUNATICCARROTBEAT",30;
      bonus2 bSubSkill,"SU_CN_METEOR",30;

  # ----- Carta Caidos + Carta Guerreiro Orc: sem resistencia a humano ---
  #
  # NASCEU EM 2026-08-10, POR DECISAO DE BALANCEAMENTO DO DONO.
  #
  # Espelha o conjunto 27328 + 4066 do db/re/item_combos.yml, com UMA
  # linha a menos. O original e:
  #
  #     bonus2 bSubRace,RC_DemiHuman,15;
  #     bonus2 bSubRace,RC_Player_Human,15;     <- esta sai
  #
  # Por que: o topo de cabeca ja oferece 13 a 16% de resistencia a raca
  # pelo proprio equipamento (o Cocar do Orc Heroi +16 da 16 via
  # RC_All), e o conjunto com a Carta Guerreiro Orc soma outros 30. Mais
  # 15 pela carta encaixada punha a cabeca sozinha acima de 60% e a
  # soma do personagem em 110% - imunidade. O bRO resolveu o mesmo
  # problema bloqueando esta carta; aqui ela continua util, so nao
  # entrega mais resistencia a jogador.
  #
  # A RC_DemiHuman FICA de proposito. Ela vale contra MONSTRO
  # humanoide e nao vale nada em PvP - jogador e RC_Player_Human
  # (battle.cpp, o race_fix do bloco "Card Fix for target"). Tirar
  # tambem seria punir o uso em campo por um problema de guerra.
  #
  # COMO ISTO SUBSTITUI O ORIGINAL: o ComboDatabase casa conjunto pela
  # LISTA DE ITENS ordenada (`find_combo_id`, src/map/itemdb.cpp), nao
  # por um id declarado. Repetir a mesma dupla num arquivo importado
  # depois troca o Script do conjunto que ja existe - nao cria um
  # segundo. Se um dia a intencao for apagar o conjunto inteiro em vez
  # de mudar o efeito, o caminho e `Clear: true` no lugar do Script.
  #
  # O QUE ISTO NAO CONSERTA: a descricao na tela. O cliente continua
  # dizendo "Resistencia as racas Humano e Humanoide +15%", porque
  # quem escreve a descricao e o itemInfo do cliente, nao o servidor
  # (§4.9 e §5 do CLAUDE.md). Corrigir a descricao e trabalho de
  # cliente, e esta em aberto.
  #
  # Recarrega com `@reloaditemdb` - conjunto entra junto com item.
  - Combos:
      - Combo:
          - LivingDeath_Card    # 27328 - Carta Caidos
          - Orc_Warrior_Card    # 4066 - Carta Guerreiro Orc
    Script: |
      bonus2 bSubRace,RC_DemiHuman,15;

  # ----- Capa do Comandante + as tres botas do Heroi: falta o Doram ----
  #
  # NASCEU EM 2026-08-10, LOGO DEPOIS DO OVERRIDE DA CAPA.
  #
  # Mesma divergencia da propria capa (20925), e a mesma correcao: a tela
  # promete "Resistencia as racas Humano e Doram +5% adicional" nos tres
  # conjuntos, e o db/re/item_combos.yml da RC_Player_Human,5 e nada para
  # Doram. Aqui o VALOR ja batia - o que faltava era so a raca.
  #
  # A promessa mora so na descricao da CAPA. Os tres calcados (22035,
  # 22036, 22037) nao citam conjunto nenhum no itemInfo.lua deste
  # cliente, nem a capa, nem "Humano" - conferido entrada por entrada.
  # E na capa que o jogador le o que vai ganhar, e e por ela que a conta
  # se fecha.
  #
  # COMO ISTO SUBSTITUI O ORIGINAL: identico a Carta Caidos, logo acima.
  # O ComboDatabase casa pela lista de itens - e `parseBodyNode` a ORDENA
  # antes de comparar (`std::sort`, src/map/itemdb.cpp), entao a ordem em
  # que se escreve aqui nao importa. Conjunto que ja existe tem o
  # `combo->script` LIBERADO e substituido pelo daqui; nao ha mesclagem
  # de linha, e por isso cada Script abaixo repete o do rAthena inteiro,
  # copiado de la, com uma linha a mais.
  #
  # Ao atualizar o rathena/, conferir os tres contra o db/re/ - se o
  # vendor mudar os numeros dele, ESTES AQUI NAO MUDAM SOZINHOS.
  #
  # Nota de alcance: as tres botas NAO estao a venda em loja nossa
  # nenhuma (`estado_item.py --id 22035,22036,22037`), entao hoje isto
  # nao muda o dano de ninguem. Entrou pela mesma razao que o resto: a
  # conta de resistencia a humano tem de fechar no item_db, nao na tela.
  # Se um dia as botas entrarem no mercado, ja chegam certas.

  # ----- Capa do Comandante + Botas de Ungoliant do Heroi -----
  # Espelha db/re/item_combos.yml, o conjunto Commander_manteau_J + 22037.
  - Combos:
      - Combo:
          - Commander_manteau_J    # 20925 - Capa do Comandante
          - Vet_Ungo_Boots    # 22037 - Botas de Ungoliant do Heroi
    Script: |
      bonus bVit,10;
      bonus bLuk,10;
      bonus bHealPower,20;
      bonus2 bSubRace,RC_Player_Human,5;
      bonus2 bSubRace,RC_Player_Doram,5;
      bonus bUseSPrate,-10;

  # ----- Capa do Comandante + Botas de Couro do Heroi -----
  # Espelha db/re/item_combos.yml, o conjunto Commander_manteau_J + 22036.
  - Combos:
      - Combo:
          - Commander_manteau_J    # 20925 - Capa do Comandante
          - Vet_SliverFox_Boots    # 22036 - Botas de Couro do Heroi
    Script: |
      bonus bStr,10;
      bonus bDex,10;
      bonus bDelayrate,-20;
      bonus2 bSubRace,RC_Player_Human,5;
      bonus2 bSubRace,RC_Player_Doram,5;

  # ----- Capa do Comandante + Sapatos Relaxantes do Heroi -----
  # Espelha db/re/item_combos.yml, o conjunto Commander_manteau_J + 22035.
  - Combos:
      - Combo:
          - Commander_manteau_J    # 20925 - Capa do Comandante
          - Vet_Nepen_Heel    # 22035 - Sapatos Relaxantes do Heroi
    Script: |
      bonus bAgi,10;
      bonus bInt,10;
      bonus bVariableCastrate,-10;
      bonus2 bSubRace,RC_Player_Human,5;
      bonus2 bSubRace,RC_Player_Doram,5;

  # ----- Touca Exotica + Carta Lady Branca -----
  # NAO espelha nada do db/re/: a Touca Exotica (400308) e peca que o nosso
  # vendor nao tem, criada em db/guerra/item_db.yml em 2026-08-12. O rAthena
  # nunca ouviu falar dela, entao este conjunto so existe aqui.
  #
  # A linha vem da descricao do bRO da propria Touca, que traz o conjunto por
  # extenso: "^FA4E09[Carta Lady Branca]^000000 / Efetividade de cura +60%
  # adicional". "Adicional" e o que decide o `bonus` ser somado e nao trocado -
  # a Touca ja da +10 de base, +15 no +7 e +15 no +9, e a carta sozinha da
  # outros +30 (db/re/item_db_etc.yml, 4372).
  - Combos:
      - Combo:
          - Exotic_traditional_Hat    # 400308 - Touca Exotica
          - Bacsojin_Card             # 4372   - Carta Lady Branca
    Script: |
      bonus bHealPower,60;

  # ----- Aura Fantasma + Sapato Fantasma -----
  # O Sapato Fantasma (470321) e peca que o nosso vendor nao tem, criada em
  # db/guerra/item_db.yml em 2026-08-20. O db/re/item_combos.yml ja traz este
  # mesmo conjunto para os quatro pes de FOR da familia - Virtue_Shoes_BR
  # (470293), aegis_470294, Lethargy_Shoes_BR (470295) e aegis_470296 -, e o
  # Script deles nao muda de pe para pe. Entao este e COPIA EXATA daquele
  # grupo, sem uma virgula de diferenca; a unica mudanca e o calcado citado.
  #
  # Sao entradas separadas, e nao uma alternativa dentro do `- Combos:` deles,
  # porque de um arquivo de import nao se acrescenta linha a uma entrada
  # alheia - a mesma razao da Diadema, no topo deste arquivo. O efeito e o
  # mesmo: so um calcado cabe por vez.
  #
  # Ao atualizar o rathena/, conferir este contra o db/re/. Se o vendor um dia
  # trouxer o 470321 de verdade, ele provavelmente trara este conjunto junto -
  # e ai esta entrada vira duplicata e sai.
  #
  # OS OUTROS QUATRO CONJUNTOS da descricao do bRO ([Bate-Estacas Motorizado],
  # [Chave Maxi], [Estal], [Injetor Acoplavel]) NAO estao espelhados: o vendor
  # nao conhece nenhum deles, entao nao ha o que copiar. Ver o TODO no bloco
  # do item em db/guerra/item_db.yml.
  - Combos:
      - Combo:
          - Subject_Aura     # 19439  - Aura Fantasma
          - Phantom_Shoes    # 470321 - Sapato Fantasma
    Script: |
      if (readparam(bStr)>=130) {
         bonus bFixedCast,-500;
         autobonus "{ bonus bStr,70; }",15,5000,BF_WEAPON;
      }

  # -----------------------------------------------------------------------
  # RAPOSA ILUSIONAL (420227) - 2026-08-26
  #
  # Os QUATRO conjuntos que db/re/item_combos.yml da ao `White_Fox_US`
  # (420314), copiados linha por linha com o nosso AegisName no lugar do
  # dele. O 420227 e o MESMO item que o 420314, renumerado pelo bRO - a
  # prova esta no cabecalho da oitava leva em db/guerra/item_db.yml.
  #
  # Sem estes quatro o item entraria na loja com os conjuntos que a
  # descricao promete mortos, e conjunto que nao fecha nao da erro: o
  # jogador so ve um numero menor. E a mesma receita da Diadema do Paraiso
  # (19455) e do Sapato Fantasma (470321), mais acima.
  #
  # As quatro pecas parceiras existem no vendor e NAO estao em loja nossa -
  # sao drop das Ilusoes. Espelhar mesmo assim e barato e correto: quem
  # trouxer a peca de fora tem o conjunto funcionando.
  - Combos:
      - Combo:
          - Long_Mace_IL          # 16063  - Maca Longa Ilusional
          - Illusion_White_Fox    # 420227 - Raposa Ilusional
    Script: |
      .@r = getequiprefinerycnt(EQI_HAND_R);
      bonus bMaxSP,200*(.@r/2);
      bonus2 bAddClass,Class_Boss,5;
      if (.@r>=9)
         bonus bNoCastCancel;
      if (.@r>=11)
         bonus2 bSkillAtk,"MO_EXTREMITYFIST",35;

  - Combos:
      - Combo:
          - Apple_Of_Archer_IL    # 19210  - Maca Ilusional
          - Illusion_White_Fox    # 420227 - Raposa Ilusional
    Script: |
      .@r = getequiprefinerycnt(EQI_HEAD_TOP);
      bonus bBaseAtk,10*(.@r/2);
      bonus bDex,3*(.@r/2);
      bonus2 bAddSize,Size_All,2*(.@r/2);

  - Combos:
      - Combo:
          - Shoes_IL              # 22133  - Sapato Ilusional
          - Illusion_White_Fox    # 420227 - Raposa Ilusional
    Script: |
      bonus bMaxHPrate,10;
      bonus bMaxSPrate,10;
      if (getequiprefinerycnt(EQI_SHOES)>=9) {
         bonus bAspdRate,10;
         bonus bFixedCast,-500;
      }

  - Combos:
      - Combo:
          - Muffler_IL            # 20838  - Sobrepeliz Ilusional
          - Illusion_White_Fox    # 420227 - Raposa Ilusional
    Script: |
      bonus bBaseAtk,50;
      bonus bMatk,50;
      if (getequiprefinerycnt(EQI_GARMENT)>=9) {
         bonus bVariableCastrate,-15;
         bonus2 bSubEle,Ele_Fire,10;
         bonus2 bSubEle,Ele_Earth,10;
         bonus2 bSubEle,Ele_Water,10;
         bonus2 bSubEle,Ele_Wind,10;
      }

  # -----------------------------------------------------------------------
  # AURA FANTASMA + GREVA FANTASMA (470309) - 2026-08-27
  #
  # Mesma receita do Sapato Fantasma (470321), logo acima, e pela mesma
  # razao: a Greva Fantasma e peca que o nosso vendor nao tem, criada em
  # db/guerra/item_db.yml na nona leva, e o db/re/item_combos.yml ja traz
  # este mesmo conjunto para os quatro pes de FOR da familia
  # (470293-470296), com um Script que nao muda de pe para pe. Entao este e
  # COPIA EXATA daquele grupo; a unica mudanca e o calcado citado.
  #
  # Ao atualizar o rathena/, conferir este contra o db/re/. Se o vendor um
  # dia trouxer o 470309 de verdade, ele provavelmente trara este conjunto
  # junto - e ai esta entrada vira duplicata e sai.
  #
  # OS OUTROS QUATRO CONJUNTOS da descricao do bRO ([Argen Blanco],
  # [Vernan], [Fortridge], [Ferramenta Dourada]) NAO estao espelhados: as
  # armas existem no vendor, mas nenhum conjunto dele casa arma com
  # calcado. Ver o TODO no bloco do item em db/guerra/item_db.yml.
  - Combos:
      - Combo:
          - Subject_Aura      # 19439  - Aura Fantasma
          - Phantom_Greave    # 470309 - Greva Fantasma
    Script: |
      if (readparam(bStr)>=130) {
         bonus bFixedCast,-500;
         autobonus "{ bonus bStr,70; }",15,5000,BF_WEAPON;
      }

  # -----------------------------------------------------------------------
  # BROCHE DA CELINE (28572) + PINGENTE DA CELINE (490482) - 2026-08-27
  #
  # O db/re/item_combos.yml casa o `Cel_Design_Pendant` (490482) com o
  # `Celine_Brooch`, que e o 28513 - e o broche que esta na NOSSA vitrine e o
  # 28572, `Celine_Brooch_BR`, o ID do bRO. Sao itens diferentes (ver o bloco
  # do 28572 em db/guerra/item_db.yml), entao o conjunto do vendor nao fecha
  # com a peca que o jogador compra.
  #
  # E os dois estao na MESMA loja, o Acessorista, a poucos cliques um do
  # outro: o Broche desde 2026-08-01 e o Pingente desde 2026-08-27. Sem este
  # espelho a descricao do Pingente prometeria um conjunto que nao acontece -
  # e conjunto que nao fecha nao da erro, o jogador so ve um numero menor. E a
  # mesma receita da Diadema do Paraiso (19455) e do Sapato Fantasma (470321).
  #
  # Script copiado sem uma virgula de diferenca do conjunto 28513 + 490482 do
  # db/re/item_combos.yml. Ao atualizar o rathena/, conferir contra o de la.
  - Combos:
      - Combo:
          - Celine_Brooch_BR     # 28572  - Broche da Celine (bRO)
          - Cel_Design_Pendant   # 490482 - Pingente da Celine
    Script: |
      bonus2 bMagicAddEle,Ele_All,25;
